#
# This file is used by build.xml 
#

# Global defaults
name=AutomaticBuildProject

build.dir=build-ant
lib.dir=lib
dist.dir=dist
build.src=src

build.jarname=${lib.dir}/${name}.jar
build.mainclass=at.fhj.iit.Main
build.distjarname=${dist.dir}/${name}.jar